MESSAGE(STATUS "\nScBridge init...")
MESSAGE(STATUS "SC_SRC_DIR : " ${SC_SRC_DIR})

#FIND_PACKAGE(SC)
FIND_PACKAGE(SCLang)

IF(SC_LANG_FOUND)
	MESSAGE(STATUS "SCmodule : " ${SC_LANG_FOUND})
        #INCLUDE_DIRECTORIES(
		#	${SC_LANG_INCLUDE_DIR}
		#	${SC_LANG_INCLUDE_DIRS}
		#)
		#LINK_DIRECTORIES(SC_LANG_LIBRARY_DIR)
ELSE()
	MESSAGE(STATUS "SCmodule - nenalezen")
ENDIF()


SET(ScBridge_src ScBridge.cpp)

#ADD_LIBRARY(ScBridge STATIC ${ScBridge_src})

ADD_LIBRARY(libsclang STATIC IMPORTED)
SET_PROPERTY(TARGET libsclang PROPERTY IMPORTED_LOCATION ${SC_LANG_LIBRARY})
 
ADD_EXECUTABLE(ScBridgeApp ${ScBridge_src})
TARGET_INCLUDE_DIRECTORIES(ScBridgeApp PUBLIC
	${SC_LANG_INCLUDE_DIR}
	${SC_LANG_INCLUDE_DIRS}
)
TARGET_LINK_LIBRARIES(ScBridgeApp libsclang)

IF(SC_LANG_FOUND)
    MESSAGE(STATUS "Linking Scalng..")
    #TARGET_LINK_LIBRARIES(ScBridge ${SC_LIBRARY})
  # TARGET_LINK_LIBRARIES(ScBridgeApp ${SC_LANG_LIBRARY})
ENDIF()

MESSAGE(STATUS "\nScBridge done...")
